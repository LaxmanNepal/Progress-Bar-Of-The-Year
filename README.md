# Progress Bar Of The Year

A lightweight, dependency-free webpage that shows the live percentage of the current year that has passed.

## Features

- Real-time year progress calculation
- Smooth opening animation
- Automatically updates every second
- Mobile-first responsive layout
- No framework or external dependency

## Deploy

Enable **GitHub Pages** from the repository settings and deploy from the default branch root.

The site will work as a static page.
